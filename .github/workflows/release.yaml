---
name: semantic-release

on:
  workflow_call:
  workflow_dispatch:
  push:
    branches:
      - master

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  build:
    # Name the Job
    name: semantic-release
    # Set the agent to run on
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        python-version: [3.8]

    # Load all steps #
    steps:
      # Checkout the code base #
      - name: Checkout Code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      # set env variable
      - name: Set env variable
        run: echo "BRANCH=${GITHUB_REF#refs/heads/}" >> $GITHUB_ENV

      - name: Install dependencies
        run: |
          python -m pip install poetry --upgrade pip
          poetry config virtualenvs.create false
          poetry install

      # Bump version
      - name: Bump version using semantic-release
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          poetry run semantic-release version

      # Can't use: relekang/python-semantic-release@master
      - name: Python Semantic Release to PyPI
        run: |
          semantic-release publish --verbosity=DEBUG --define=branch=${{ env.BRANCH }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPOSITORY_USERNAME: __token__
          REPOSITORY_PASSWORD: ${{ secrets.REPOSITORY_PASSWORD }}

      # # semantic release
      # - name: Python Semantic Release to PyPI
      #   uses: relekang/python-semantic-release@master
      #   with:
      #     github_token: ${{ secrets.GITHUB_TOKEN }}
      #     repository_username: __token__
      #     repository_password: ${{ secrets.PYPI_API_TOKEN }}
      #     additional_options: --verbosity=DEBUG --define=branch=${{ env.BRANCH }}
